input.form-control.minicolors type="text" value=(value) name=(form_field_name(name)) id=(form_field_id(name)) required=(required)

javascript:
  // TODO: This shouldn't run multiple times
  $(function() {
    $('input.minicolors').minicolors({
      theme: 'bootstrap'
    });
  })
