input.form-control.minicolors type="text" value=(value) name=(form_field_name(field)) id=(form_field_id(field)) required=(required)

javascript:
  // TODO: This shouldn't run multiple times
  $(function() {
    $('input.minicolors').minicolors({
      theme: 'bootstrap'
    });
  })
